version: "3.8"

services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    ports:
      - "8070:80"
    volumes:
      - ./data:/srv
      - ./database:/database
      - ./config:/config
    restart: unless-stopped